/** Sample queries for Commodity Trading business network
*/

query selectPackage {
  description: "Selection du Package depuis un tagid"
  statement:
      SELECT org.example.mynetwork.Package
       WHERE (tag == _$tagid)
}
